export enum TestRoutes {
    ROOT = '/api/v1',
    QUIZ = '/quiz',
    QUESTION = '/question',
}
export enum TestQuizEndpoints {
    CREATE = '/create',
    ALL = '/all',
    DELETE = '/delete',
}
export enum TestQuestionEndpoints {
    CREATE = '/create',
}
